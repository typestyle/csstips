{
  "name": "csstips",
  "version": "0.0.2",
  "description": "",
  "main": "./lib/index.js",
  "typings": "./lib/index",
  "scripts": {
    "build": "tsc -p .",
    "test": "npm run build",
    "preversion": "npm run build",
    "postversion": "git push --follow-tags && npm publish",
    "start": "npm run build -- -w"
  },
  "author": "",
  "license": "ISC",
  "peerDependencies": {
    "typestyle": "*"
  },
  "devDependencies": {
    "typestyle": "*"
  }
}
